<!DOCTYPE html>
<html lang="es" dir="ltr">
  <head>
	<link rel="stylesheet" href="/CSS/cabecera.css">
    <link rel="stylesheet" href="/CSS/practica.css">
	<link rel="stylesheet" href="/CSS/modify_user.css">
    <meta charset="utf-8">
    <title>Modificar usuario - FaceBluff</title>
  </head>
  <body>
		<%- include("headerLogged.ejs") %>
		<div class="content">
			<div class="titulo">
				<h1>Modificar usuario</h1>
			</div>
			<div class="seccion">
				<div class="newUser">
						<% if (errors != null) { %>
							<div class="contError">
								<ul>
								<% errors.forEach(error=>{ %>
									<li><span class="error"><%= error.msg %></span></li>
								<% }) %>
								</ul>
							</div>
						<% } %>
					<form action="/doModify" method="POST" class="formularioModifyUser" enctype="multipart/form-data">
						<div class="contDatosModifyUser">
							<span>Email</span><span><input name="email" type="text" class="inputText" value= <%= currentUser.email %> /></span>
							<span>Contraseña</span><span><input name="password" type="password" class="inputText" placeholder="Dejar en blanco si no se quiere cambiar"/></span>
							<span>Nombre completo</span><span><input name="name" type="text" class="inputText" value= '<%= currentUser.name %>' /></span>
							<span>Sexo</span>
							<span>
								<input type="radio" name="gender" class="inputRadio" value="Hombre" <%= currentUser.gender=="Hombre" ? "checked":"" %> /> Masculino
								<input type="radio" name="gender" class="inputRadio" value="Mujer" <%= currentUser.gender=="Mujer" ? "checked":"" %> /> Femenino
								<input type="radio" name="gender" class="inputRadio" value="Otro" <%= currentUser.gender=="Otro" ? "checked":"" %> /> Otro
							</span>
							<span>Fecha de nacimiento</span><span><input name="birth" type="date" class="inputText" value='<%= currentUser.birthdate %>' /></span>
							<span>Imagen de perfil</span><span><input name="image" type="file" class="inputArchivo" accept="image/*" placeholder="Prueba"/></span>
							<span>Borrar Imagen</span><span><input name="deleteImage" type="checkbox" value='false'/></span>
						</div>
						<div class="contBoton">
							<span><input type="submit" value="Guardar Cambios" class="boton"/></span>
						</div>		
					</form>
				</div>
			</div>
		</div>
  </body>
</html>
